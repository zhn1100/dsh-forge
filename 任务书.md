DSH Forge：DeepSeek Harness 自举式插件开发实例任务书

1. 项目定义

项目暂定名：DSH Forge

DSH Forge 是一个基于 DeepSeek Harness / Cordis 的专用开发型 DSH 实例。

其目标不是给普通项目中的 Agent 增加一个“会写 Harness 插件”的 Skill，而是将某一个独立 DSH 实例整体特化为：

«能够理解当前版本 DeepSeek Harness 的开发规则，按需检索其完整开发文档与源码参考，检查自身运行时结构，设计、实现、加载、验证、调试和迭代 Cordis/Harness 插件，并最终将成功的运行时原型转化为可重建、可安装、可验证的正式插件的软件创造环境。»

最终希望形成：

需求描述
   ↓
Harness/Cordis 架构解析
   ↓
权威开发知识检索
   ↓
运行时检查
   ↓
插件方案设计
   ↓
原型构造
   ↓
动态加载 / 测试
   ↓
失败诊断
   ↓
版本迭代
   ↓
正式 Package / Bundle
   ↓
干净 Profile 验证

本项目服务于创造 DSH 本身及其插件生态，不作为普通 coding project 的默认运行环境。

---

2. 设计依据

DeepSeek Harness 本身是一棵由 Profile、Bundle、patch 和 Cordis plugin 组合出的运行树。"web" 和 "headless" 也只是随发行版提供的两种 Profile；Bundle 贡献配置层，Profile 决定 Bundle 的组合顺序，最终所有层仍可继续被上层 patch 覆盖。

Cordis 插件具有独立 Fiber 生命周期，依赖 Service 缺失时保持 PENDING；依赖消失后消费插件会自动 dispose，并在依赖恢复后重新加载。通过 "ctx" 注册的事件、Tool、LLM adapter 和自定义 effect 均绑定插件生命周期，HMR 会先卸载旧实例、回卷 effect，再载入新实现。

Harness 明确支持 Service Definition / Service Provider / Consumer 三角色能力 seam，并支持通过 "isolate" 为不同组提供同名 Service 的独立实例。因此 Forge 可以使用独立 scope、独立 Provider 和专用 Agent preset，而不要求修改核心 agent loop。

当前 DSH 已提供显式 opt-in 的 "@deepseek-ai/dsh-tool-cordis"，包括 "cordis_define"、"cordis_run"、"cordis_stop"、"cordis_undefine" 与三项 inspect 工具。动态 Package 使用不可变 Package 版本；修改现有 Plugin 实际上是追加新 Package，而不是覆盖旧版本。该工具默认不进入产品组合，因为动态 Package 可以接触真实运行时。

动态 Cordis 还提供 Host/Client Inspect：模型在创建 Package 前可以先枚举 Inspector，再查询精确 Service、Event、Builtin、Tool Schema、Slot 等运行时约定，而不是凭记忆猜测 API。

因此 DSH Forge 不需要重新创造“Agent 修改 Cordis”的机制，而应重点建设：

«知识、设计、验证、隔离、版本管理和正式化这五层能力。»

---

3. 总体目标

Forge 应满足四个核心目标。

3.1 Knowledge-complete

Agent 不要求把所有开发文档塞入上下文，但对于任何 Harness 开发问题，都应能够定位到当前版本对应的：

- 开发指南；
- Cordis API；
- Service；
- Event；
- Tool Schema；
- Config；
- Persistence Event；
- capability seam；
- package；
- TypeScript symbol；
- 已有实现示例；
- 测试与验证约束。

所谓“完整理解文档”，在工程上定义为：

«任何设计决定都能够按需恢复其所依赖的权威开发知识，而不是依赖模型预训练记忆。»

3.2 Runtime-aware

Forge Agent 必须能够区分：

文档描述的 Harness
        与
当前真正运行的 Harness

在修改运行环境前，应先通过 Inspect、Fiber registry、实际配置树以及必要的源码检查获得当前运行时事实。

3.3 Self-iterative

对于实验插件，应能够形成：

inspect
→ design
→ define
→ run
→ observe
→ diagnose
→ revise
→ update / rollback

闭环。

3.4 Reproducible

成功的动态原型不能作为最终成果。

最终成果必须能够脱离当前 live runtime，重新表达为：

source
+ package manifest
+ Config
+ cordis patch
+ Bundle / Profile composition
+ tests

并在一个新的干净 DSH 实例中重新构建和通过验证。

---

4. 非目标

第一阶段不追求以下能力：

- 任意修改 DSH Forge 自己的控制平面；
- 自动修改 DSH 核心 agent loop；
- 自动发布 npm；
- 自动向 DeepSeek Harness 上游提交 PR；
- 为任意第三方 npm 插件提供强安全保证；
- 将动态 Cordis Package 直接视为正式发行物；
- 完整解决 Cordis capability security；
- 使用大规模向量数据库替代源码和生成参考。

Forge v0.x 的定位是：

«受控、自举、可恢复的 Harness 插件实验与开发环境。»

---

5. 实例隔离方式

Forge 不应直接安装进用户普通 "web" Profile。

建议使用独立：

DSH_HOME
+
forge Profile
+
forge Bundle
+
forge Agent Preset

形成：

~/.dsh/
    普通工作环境

~/.dsh-forge/
    Forge 专用环境

理由是 Profile 虽然拥有自己的树外依赖和 profile patch，但 Home 级 "cordis.patch.yml" 位于 Profile 层之后并被多个 Profile 共享；彻底分离 "DSH_HOME" 可以隔离开发插件与控制面，同时允许把普通 Home 作为只读上游，同步用户态配置、凭据和会话数据。Profile 与 Bundle 的职责边界由 DSH 启动模型明确规定。

Forge 在安装时及每次启动时执行单向、基线感知的 Home 同步：目标文件不存在且从未同步过时复制；目标文件仍等于上次同步基线时接收上游更新；目标文件已修改或已删除时保留 Forge 侧状态并记录冲突。普通 Home 中的删除不向 Forge Home 传播。"forge/"、"profiles/"、".agent-presets/" 与 "cordis.patch.yml" 永久排除，符号链接不跟随。同步清单位于 "~/.dsh-forge/forge/home-sync-manifest.json"。

建议启动概念：

DSH_HOME=~/.dsh-forge
dsh --profile forge

最终 Forge 应能够在完全不修改普通 DSH Home 的情况下安装、升级和移除。

---

6. 总体架构

┌──────────────────────────────────────┐
│             Forge Profile            │
│                                      │
│  ┌────────────────────────────────┐  │
│  │       Forge Control Plane      │  │
│  │                                │  │
│  │ Docs / Reference              │  │
│  │ Source Index                  │  │
│  │ Runtime Inspector             │  │
│  │ Experiment Supervisor         │  │
│  │ Verification Engine           │  │
│  │ Promotion Manager             │  │
│  │ Recovery                      │  │
│  └───────────────┬────────────────┘  │
│                  │                   │
│       controlled experimental seam  │
│                  │                   │
│  ┌───────────────▼────────────────┐  │
│  │      Experimental Plane        │  │
│  │                                │  │
│  │ Dynamic Cordis Plugin A/v1    │  │
│  │ Dynamic Cordis Plugin A/v2    │  │
│  │ Dynamic Cordis Plugin B/v1    │  │
│  │ temporary Service / Tool / UI │  │
│  └────────────────────────────────┘  │
│                                      │
│  Agent Preset: cordis-developer      │
└──────────────────────────────────────┘

控制平面在第一阶段不得由实验插件修改。

实验失败必须只能破坏 Experimental Plane，而不能破坏负责停止、回滚、诊断和重新启动实验的 Forge Supervisor。

---

7. Forge Knowledge Plane

7.1 数据源

知识系统按权威级别分为：

Level 0  当前源码和运行时 Inspect
Level 1  自动生成 Reference
Level 2  官方 Development / Architecture 文档
Level 3  package README / Agent Notes
Level 4  示例、测试、历史实现

自动生成 Reference 应作为主要数据库来源，因为 Harness 已经从源码生成 Tool Schema、Config 和 Cordis API，并使用 "doc-sync"/对应 verifier 检查同步状态；Tool Catalog 的生成甚至会实际挂载 Tool 插件读取 "ctx.tools.schemas()"，而不是仅做静态文本扫描。

7.2 不采用“全文常驻 Prompt”

Forge 不应把全部文档永久放入 System Prompt。

常驻 Prompt 只保存：

Cordis 开发原则
能力归属规则
查文档规则
Inspect-before-implement 规则
验证纪律
动态原型与正式包区别
安全与回滚规则

具体 API、Schema、Config 和源码细节按需检索。

System Prompt 本身支持有序 section、scope 局部覆盖和动态 context，因此开发人格可以独立安装到 Forge preset，而不污染其他 Agent。

7.3 检索结构

第一版不以 embedding 为核心。

优先建立结构化索引：

Document
Symbol
Package
Service
Provider
Consumer
Tool
Event
Config
SessionEvent
CapabilitySeam
SourceLocation
Example
Test

需要支持至少以下查询：

forge_docs_search(query)

forge_symbol(name)

forge_service(ctxKey)

forge_event(name)

forge_tool(name)

forge_config(package)

forge_package(name)

forge_related(symbol)

forge_source(symbol)

forge_example(capability)

语义搜索只作为不知道确切术语时的补充。

---

8. Runtime Inspector

任何插件开发流程必须先执行：

Inspect → Design

而不是：

Design → 猜 API → Compile

动态 Cordis 已明确要求创建或修改 Package 前先调用 "cordis_inspect_list"，再通过 "cordis_inspect_query" 查询实际 API；Forge 应把这一要求提升为全局开发纪律。

Inspector 应整合：

- "cordis_inspect_*"；
- Fiber registry；
- 当前 Plugin/Fiber 状态；
- 当前 "ctx" Service；
- Service provider；
- Event signature/mode；
- Tool registry；
- Prompt sections；
- Agent scope；
- 当前 Profile 展开的配置树；
- package/source revision。

重点诊断状态：

PENDING
FAILED
ACTIVE
DISPOSED

尤其不得把长期 PENDING 插件误判为“成功加载”。Cordis 文档明确指出缺少 inject provider 时插件可能合法地长期 PENDING。

---

9. Forge Developer Agent

建立专用 Agent Preset：

cordis-developer

Preset 只在 Forge Profile 使用。

其 Standing Composition 至少包含：

Forge development prompt
Forge docs tools
Filesystem
Search
Bash
LSP
Workflow
Subagent
Dynamic Cordis
Runtime Inspect
Verification tools
Git tools

Agent preset 本身可以挂载 scope-local composition；Harness 的 preset 发现机制会在 Agent 创建阶段将对应 "cordis.yml" 挂到该 Agent scope，并拒绝不合法地向 root service realm 发布服务的条目。

Skill 系统则用于存放较长、低频的专项开发流程，例如：

develop-tool
develop-service
develop-provider
develop-ui
develop-hook
develop-persistence
develop-agent-preset

Skill 正文按需加载而不是全部常驻；当前 Skill 注册表本身就是 Host + scope 分层结构，preset scope 可以覆盖全局同名 Skill。

---

10. 插件开发状态机

每个 Forge Development Task 使用统一状态机：

REQUEST
   ↓
CLASSIFY
   ↓
INSPECT
   ↓
RETRIEVE
   ↓
DESIGN
   ↓
PLAN CHECK
   ↓
IMPLEMENT
   ↓
STATIC VERIFY
   ↓
PROTOTYPE
   ↓
RUNTIME VERIFY
   ↓
┌───────────────┐
│ PASS          │ FAIL
│               ↓
│          DIAGNOSE
│               ↓
│           REVISE
│               │
└──────◄────────┘
   ↓
PROMOTE
   ↓
CLEAN PROFILE TEST
   ↓
DELIVER

---

11. Design 阶段要求

Agent 在写代码之前必须生成内部的 "PluginDesignSpec"。

至少回答：

目标能力是什么？

属于：
- Tool
- Hook
- Service
- Provider
- Consumer
- UI
- Agent preset
- persistence
- workflow
- other

是否已经存在对应 capability seam？

是否需要新 Service？

是否需要 Definition / Provider / Consumer 三拆？

作用域：
- host-global
- profile
- preset
- agent
- transient experiment

生命周期所有者是谁？

会产生哪些 effect？

有哪些依赖 inject？

需要哪些 Event？

是否修改模型可见上下文？

是否需要 Config？

是否需要 Client half？

安全边界是什么？

如何验证？

如何卸载和回滚？

不得为了“架构漂亮”预防性三拆。Harness 开发文档明确要求只有 Definition / Provider / Consumer 需要独立演进时才拆包；单用途插件保持单包。

---

12. 双路径实现策略

Forge 支持两类开发路径。

Path A：Dynamic Prototype

适用于：

- Tool 原型；
- Hook；
- Service；
- Event listener；
- runtime behavior；
- 简单 UI 实验；
- API 可行性验证。

利用：

cordis_define
cordis_run
cordis_inspect_self
cordis_stop
cordis_undefine

动态 Package 必须视为不可变 revision：

Plugin P
 ├── Package p1
 ├── Package p2
 ├── Package p3
 └── current → p3

失败后追加新 Package，不覆盖旧 Package；需要回滚时重新运行旧 Package。该语义由当前 Cordis 动态工具直接提供。

必须注意，"cordis_define" 接收的是返回 Cordis Plugin 的 plain JavaScript 函数体，不进行 TypeScript、JSX 或 import 转换，因此动态原型不能替代真实 TypeScript package 构建流程。

Path B：Source-first Development

适用于：

- 复杂 TypeScript 插件；
- 多 package capability；
- Client UI；
- 编译依赖复杂；
- 需要测试；
- 最终正式插件。

流程直接在 workspace / worktree 内创建源码，然后：

HMR
+
tests
+
clean profile

动态 prototype 为可选步骤，不是硬要求。

---

13. Verification Plane

Forge 必须把“模型认为写好了”和“Harness 认为合法”分开。

正式 Package 最低验证门应对齐官方 package checklist：

pnpm install
pnpm run doc-sync
pnpm run constraints
pnpm run typecheck
pnpm run lint
pnpm run build
pnpm run hygiene
专项测试

这是当前官方新增 workspace package 的明确验证路径。

Forge 应把这些命令封装成：

forge_verify(level)

建议级别：

quick
    targeted typecheck/lint/test

package
    package-related validation

full
    官方完整 gate

clean
    独立 Profile / fresh process 集成验证

---

14. Runtime Verification

静态检查通过后还必须进行运行时验证。

验证至少包括：

Fiber 是否 ACTIVE
inject 是否满足
Service 是否正确发布
Tool schema 是否存在
Event 是否按正确 mode 注册
Prompt section 是否正确出现
卸载后 effect 是否消失
重新加载是否无重复注册
失败更新是否保留旧 current
rollback 是否恢复旧行为

对于适合 runtime invariant 的正式 package，还应生成或检查对应 invariant contribution。Harness 当前已有 "ctx.invariants" 注册表，并要求 package 自己拥有可检查的不变式，而不是由中央系统猜测业务约束。

---

15. Workflow 与 Subagent

复杂开发任务不应全部由一个 Agent 串行完成。

Forge 可利用当前 Workflow seam，让主 Developer Agent 动态生成编排脚本并启动受父 Agent 所有的子 Agent。当前 Workflow Provider 使用 worker thread，每个 run 一个 worker，并具有有界取消和 dispose 语义。

建议逻辑角色：

Architect
Implementation
Reference Reviewer
Test Designer
Runtime Diagnostician

但这些是任务角色，不要求永久存在五个固定 Agent。

由主 Agent 根据复杂度动态决定是否展开。

---

16. Control Plane 与 Experimental Plane 隔离

第一阶段必须设置硬边界：

Forge Control Plane
    不允许 Dynamic Cordis 修改

Experimental Plane
    允许模型创建与更新

控制平面包括：

Docs Index
Supervisor
Verifier
Recovery
Experiment Registry
Promotion Manager
Authorization Policy

实验插件不得：

- 卸载 Forge Supervisor；
- 替换 Forge Docs；
- 修改 Forge Recovery；
- 修改权限策略；
- 获取未授权 credential；
- 修改普通 DSH Home。

即使 Cordis 哲学上不存在 privileged application core，Forge 自举开发环境仍需要人为定义一个暂时可信控制域，否则错误实验可能同时破坏执行者与修复者。

---

17. 安全策略

动态 Cordis 必须维持 opt-in，只存在于 Forge Profile。官方本身没有把 "dsh-tool-cordis" 加入 shipped tree，原因就是动态 Package 可以访问真实 runtime。

Client Package 的运行必须保留现有审批链。当前 "cordis_run" 在 Client Package 未授权时会进入 approval，而不是直接激活；用户拒绝后不得由 Agent 自动反复请求。

Shell/进程执行不能把当前 Sandbox 当作完整安全容器。现有 "ctx.sandbox" 主要定义文件系统效果限制，网络和进程可见性明确不属于这套 vocabulary；并且后端可能报告 "partial" enforcement。

因此 Forge 第一阶段主要依赖：

独立 Home
独立 workspace
Git worktree
最小 credential
审批
可回滚源码
Cordis lifecycle
进程 sandbox

而不是宣称动态代码已经获得强隔离。

另外，禁止 Agent 在无人确认的情况下对不可信 git dependency 开启 pnpm "allowBuilds"。官方文档明确指出 git dependency 的 "prepare" 构建脚本会在 agent sandbox 之外于宿主机执行。

---

18. Experiment Registry

Forge 自己维护实验元数据：

Experiment
  id
  task
  baseCommit
  profileRevision
  documentationRevision
  designSpec
  pluginId
  packageRevisions[]
  currentRevision
  verificationRuns[]
  runtimeDiagnostics[]
  promotionStatus

任何一次自动迭代必须可回答：

为什么产生这个版本？
它依据了哪些文档？
它改变了什么？
上一个版本是什么？
测试结果是什么？
当前 live 版本是什么？
是否已经正式化？

---

19. Promotion

"PROMOTE" 的定义不是“当前动态插件跑通”。

而是：

«将运行成功的设计转写为源码控制下的正式 Harness 软件实体。»

Promotion 至少需要产生：

package.json
tsconfig.json
src/
README
Config schema
tests
optional invariant
cordis.patch.yml
bundle manifest（若需要）

正式包的命名、拓扑、README、Model Experience 和 workspace constraints 应遵守当前 Harness package cookbook。

完成后必须离开当前 live experiment，通过新进程 + clean Forge test profile 重建。

如果只有原 runtime 能跑，而 clean profile 不能跑，则 Promotion 失败。

---

20. Forge 自身模块建议

Forge v0.1 建议拆成以下组件：

forge-core
    公共类型与 task state

forge-docs
    文档/Reference 数据模型

forge-docs-local
    本地 checkout Provider

tool-forge-docs
    面向模型的查询 Consumer

forge-source-index
    symbol/source relationship

forge-inspector
    文档世界与 live runtime 的桥接

forge-experiment
    Dynamic Cordis experiment lifecycle

forge-verifier
    build/test/doc-sync/constraints

forge-workspace
    worktree 与插件源码工作区

forge-promote
    dynamic design → formal package

forge-policy
    Forge Control Plane protection

forge-bundle
    Forge composition

cordis-developer preset
    专用开发 Agent

第一版不必全部独立发布为 package。

遵循 Harness 自身原则：

«只有需要独立替换和演进的角色才拆分。»

---

21. 第一阶段 MVP

MVP 不追求“自主开发任意 Harness 组件”。

只验证完整闭环。

输入：

«创建一个新的 Harness Tool，接受一个字符串参数，调用一个已有 Service，并以规范 Tool result 返回。»

Forge 必须自行完成：

识别任务类型
↓
查 Tool 开发文档
↓
查现有 Service API
↓
Inspect 当前 runtime
↓
产生设计
↓
实现 prototype
↓
动态运行
↓
实际调用测试
↓
主动制造一次错误版本
↓
读取诊断
↓
产生下一 Package
↓
成功更新
↓
回滚测试
↓
生成正式 TypeScript package
↓
通过官方验证 gate
↓
安装进 clean Profile
↓
再次测试成功

如果这条链能够全自动完成，项目核心架构即成立。

---

22. 第二阶段目标

扩展到四类测试能力：

A. 单插件 Tool
B. Service + Consumer
C. Hook / Policy
D. Host + Client UI

尤其 C 和 D 可以暴露第一阶段无法发现的作用域、审批和 Host/Client 生命周期问题。

---

23. 第三阶段目标

加入更加复杂的“创造模式”：

需求
↓
自动判断是否应复用现有 seam
↓
生成 architecture proposal
↓
多 Agent review
↓
自动形成 Package topology
↓
实现多个 package
↓
组合成 Bundle
↓
创建测试 Profile
↓
完整回归

此时 Forge 才开始接近真正的：

«Harness implementation compiler»

即把相对高层的软件能力描述转化成真实 Cordis composition。

---

24. 验收标准

MVP 验收

必须全部满足：

1. Forge 使用独立 DSH Home；
2. 普通 Profile 中不存在动态 Cordis Tool；
3. Agent 可以查询当前版本开发文档；
4. Agent 可以精确查询 Service/Event/Tool，而不是只做全文搜索；
5. 开发前自动 Runtime Inspect；
6. 能创建动态 Plugin；
7. 能追加 immutable Package revision；
8. 能检测 FAILED/PENDING；
9. 能自动修复并 update；
10. 能 rollback；
11. stop 后实验 Tool/Service 不残留；
12. 能将原型转为正式 TypeScript package；
13. 正式 package 通过官方核心验证 gate；
14. 能在 fresh process / clean profile 重现功能；
15. 全过程保留 experiment trace；
16. 不修改普通用户 DSH Home。

知识系统验收

随机抽取 Harness 开发问题：

Tool
Service
Event
Config
Prompt
Skill
Workflow
Sandbox
Persistence
Client UI

Forge 必须能够给出：

对应权威文档
对应源码 symbol
对应现有实现
适用扩展点

无法定位时必须明确报告“未找到”，不能凭预训练知识编造 API。

---

25. 失败定义

以下任意情况均视为系统级失败，而不仅是一次 Agent 失败：

动态实验破坏 Forge Supervisor
插件 stop 后存在残余 Tool/Listener/Service
无法知道当前运行的是哪个 revision
动态实现无法转成源码实现
clean profile 无法重现
Agent 使用不存在的 Cordis API
文档版本与源码版本无法对应
验证失败却被标记为完成
PENDING 被视为 ACTIVE
需要人工删除 Forge Home 才能恢复
普通 DSH Profile 被实验插件污染

---

26. 版本与文档一致性

Forge Knowledge Store 必须记录：

Harness commit
Harness version
documentation revision
index build revision

开发任务开始时形成 Knowledge Snapshot。

如果任务过程中仓库版本变化：

不得静默混合两个版本的 API 知识。

应重新索引或继续锁定原 revision。

这也是 Forge 与普通 RAG coding assistant 的一个重要区别：

«Reference 不是“背景知识”，而是当前软件世界的类型信息。»

---

27. 最终形态

第一阶段：

DSH
+
专用 Forge Bundle
+
Developer Preset

第二阶段：

DSH Forge Profile

成熟以后：

DSH Forge
≈
Cordis-native development environment

它所接受的输入逐渐从：

“写一个某某 TypeScript 插件”

提升到：

“让这个 Harness 获得某项能力”

而 Forge 自己负责回答：

能力应该属于哪里？
应该复用哪个 seam？
需要哪些作用域？
需要哪些生命周期？
应该生成几个组件？
如何运行？
如何验证？
如何撤销？
如何正式化？

因此本项目最终检验的不是 Agent 是否“会写插件代码”，而是：

«DSH 是否能够把自身的开发知识、运行时结构和 Cordis 的动态组合能力闭合成一个可持续的软件创造循环。»

---

28. 第一轮实际开发顺序

建议严格按以下顺序推进：

P0  独立 forge Profile / DSH_HOME
P1  Docs + Reference 本地检索
P2  Runtime Inspect 统一接口
P3  cordis-developer Agent preset
P4  Dynamic Cordis Experiment Manager
P5  Verification Engine
P6  MVP 单 Tool 自举闭环
P7  Source Promotion
P8  Clean Profile Reproduction
P9  Service + Consumer 测试
P10 Host/Client 实验
P11 Control Plane protection
P12 多 Agent / Workflow 创造流程

在 P8 之前不增加复杂 UI。

在 P6 之前不做向量数据库。

在 P10 之前不允许 Forge 自修改 Control Plane。

在完整闭环建立之前，不做插件市场、自动发布或上游 PR 自动化。

---

29. v0.1 落地修订与验收边界（2026-08-14）

本项目首个实现锁定：

- npm 运行时：`@deepseek-ai/dsh@0.1.0-rc.6`；
- 官方源码 / 文档 revision：`47f943859bef60e4160492346772ded9b24f765a`；
- 索引格式：Forge Knowledge Index v1。

当前官方 revision 的仓库根 `package.json` 仍写作 `0.1.0-rc.5`，而 npm 已发布
`0.1.0-rc.6`。Forge 不把二者静默归并为一个“版本”，Knowledge Snapshot 同时记录：

- `harnessVersion`：源码 manifest 版本；
- `runtimePackageVersion`：实际运行的 npm 包版本；
- `harnessCommit` / `documentationRevision`：源码与文档 revision。

升级时必须同时更新上述 pin 并重建索引。版本字段不一致本身不判定为索引损坏，
但不得隐藏。

动态 Cordis 工具以源码和生成 Tool Catalog 为准。当前源码实际注册：

- `cordis_inspect_list`；
- `cordis_inspect_query`；
- `cordis_inspect_self`；
- `cordis_define`；
- `cordis_run`；
- `cordis_stop`；
- `cordis_undefine`。

如果 package README 中的工具数量概述与源码不一致，Forge 必须报告这一差异并采用
“当前运行时 Inspect + 当前 revision 源码”作为权威事实，不得仅按概述删减能力。

官方 monorepo 的 `doc-sync`、`constraints` 与 `hygiene` 不能机械要求每个树外插件都提供
同名脚本。Forge 的 `full` gate 会执行目标 package 实际声明的官方同名 gate，缺失项明确
列为 `skipped`；不得把跳过项伪装为成功命令。正式回植 Harness monorepo 时仍须执行第 13
节的全部官方 gate。

clean Profile 验收必须安装 Forge Home 内按内容 SHA-256 命名并保存的 tarball，不得以指向开发
工作区的 `link:` 作为最终结果，也不得让相同路径复用旧包管理器缓存。源码 checkout 可以变化
或离线，已安装 profile 仍须能从该不可变 tarball 启动。

独立 Home 会通过上述只读、冲突安全的启动同步吸收普通 Home 的凭据和用户态配置；若源 Home
没有可用凭据，涉及真实模型调用、Client-half 人工审批以及完整“主动制造错误版本 → 自动修复
→ rollback”的 MVP 场景，仍须在用户为 Forge Home 显式配置模型后执行。无凭据的软件验收覆盖：构建、结构化索引、精确查询、状态机、不可变 revision
记录、Promotion scaffold、tarball 安装、配置展开、fresh process 启动、HTTP 可达和普通
Home 零修改；不得用这部分结果冒充已完成真实模型闭环。
